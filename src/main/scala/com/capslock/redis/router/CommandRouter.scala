package com.capslock.redis.router

import akka.actor.{Actor, ActorLogging}

/**
  * Created by capsl on 2016/2/8.
  */
class CommandRouter extends Actor with ActorLogging{
  override def receive: Receive = {
    case _ =>
  }
}
